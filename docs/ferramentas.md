# Ferramentas

MTE: Processos por Empregador expõe 1 ferramenta (todas somente leitura).

### 1. `mte_processo_empregador_consultar`
**Input**: `cnpj_raiz` (opcional), `cnpj` (opcional), `cpf` (opcional)

MTE: Processos por Empregador, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar MTE: Processos por Empregador
```
